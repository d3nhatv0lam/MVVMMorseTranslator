<UserControl x:Class="MVVMMorseTranslator.Views.MorseTranslatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMMorseTranslator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="750">

    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel Orientation="Horizontal">
            <StackPanel Orientation="Vertical" Margin="15 30 0 0 " Width="500">
                <Label Content="Alphabet" FontSize="50"></Label>
                <TextBox Text="{Binding Alphabet, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" FontSize="50"></TextBox>

            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="15 30 0 0" Width="500">
                <Label Content="MorseCode" FontSize="50"></Label>
                <Button Content="Delete File" Command="{Binding DeleteAudio}" ></Button>
                <TextBox Text="{Binding MorseCode, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" FontSize="50"></TextBox>
            </StackPanel>
        </WrapPanel>
        
    </Grid>
</UserControl>
